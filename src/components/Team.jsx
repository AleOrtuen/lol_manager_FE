import { useSelector } from "react-redux";

function Team() {

    const user = useSelector((state) => state.user);

    useEffect(() => {
        

    }, []);

    return(
        <></>
    )
}

export default Team;